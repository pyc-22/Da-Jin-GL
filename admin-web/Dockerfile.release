ARG NGINX_IMAGE=m.daocloud.io/docker.io/library/nginx:alpine
FROM ${NGINX_IMAGE}
COPY admin-web/nginx.conf /etc/nginx/conf.d/default.conf
COPY admin-web/dist /usr/share/nginx/html
EXPOSE 80
HEALTHCHECK --interval=15s --timeout=5s --retries=10 CMD wget -q -O /dev/null http://127.0.0.1/ || exit 1
