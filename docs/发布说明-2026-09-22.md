# 打金店管理系统发布说明（2026-09-22）

## 本次内容

- 移动端加工订单在完成状态支持上传取货照片，后台加工订单详情可查看照片存档。
- 确认取货时校验尾款已结清且至少存在一张取货照片。
- 移除移动端首页横向滑动切换底部页面的行为，保留滑动后的误点击抑制。
- 修复移动端 H5 同源 API 与 81 端口跨域配置。
- 恢复管理端黄金、白银实时行情显示。
- 修正发布 Dockerfile 与 `docker-compose.release.yml`，服务器可直接从 GitHub 源码构建。

## 构建与测试

- 管理端：21 项测试通过，生产构建成功。
- 移动端：64 项测试通过，H5 生产构建成功。
- 收银端：46 项测试通过，生产构建和 Windows NSIS 安装包生成成功。
- 后端：68 项测试通过，1 项条件性测试跳过，Spring Boot JAR 构建成功。
- Docker：backend、admin-web、mobile-web 发布镜像从源码构建成功。

## 发布文件

- 源代码：GitHub 仓库 `main` 分支。
- Windows 收银端：`打金店收银台 Setup 1.0.0-rc.1.exe`，作为 GitHub Release 附件上传。
- 服务器部署：使用 `docker-compose.release.yml` 和 `.env.production.example`。

安装程序暂未配置商业代码签名证书，Windows 可能显示“未知发布者”，可用随附 SHA-256 文件校验完整性。
