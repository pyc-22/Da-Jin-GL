# V1.0.0-rc.1 构建测试报告

构建日期：2026-09-19

## 2026-09-20 收银端白屏修复复测

- 原安装产物在 Electron `file://` 下复现：JS 和 CSS 请求落到磁盘根目录的 `assets`，出现两条 `net::ERR_FILE_NOT_FOUND`，Vue 未挂载、样式未加载。
- 修复：Vite `base: './'`；`pnpm run build` 自动检查每条生成的 JS/CSS 引用是否解析到真实的 `dist` 文件。
- `pnpm test`：收银端 11 个测试文件、30 项测试通过。
- `pnpm run dist`：Vite 构建、资源验证、Windows x64 NSIS 安装程序重新生成成功。
- `pnpm exec electron scripts/verify-electron-package.cjs`：使用临时用户目录，在 Electron 中运行新 ASAR 的主进程入口及正式 `loadFile` 分支；页面、样式、预加载接口正常，本地资源失败数为 0。
- 同一检查运行旧 ASAR 得到失败，运行新 ASAR 得到通过，已留存对照报告与截图于开发工作区 `runtime-logs/cashier-package-smoke*`。
- 本次仅重建收银端；下表其余模块为 2026-09-19 的测试记录。后端业务、安装向导、真机打印仍按部署后验收执行。

## 环境

- Windows 11
- Java 17.0.20.1
- Maven Wrapper 3.9.6
- Node.js 24.19.0
- pnpm 11.19.0

## 自动测试

| 模块 | 结果 | 数量 |
|---|---|---:|
| Spring Boot 后端 | 通过 | 49 |
| Vue3 管理端 | 通过 | 20 |
| 移动端 H5 | 通过 | 43 |
| Electron 收银端 | 通过 | 30 |
| 合计 | 通过 | 142 |

## 构建结果

| 制品 | 结果 |
|---|---|
| `backend-1.0.0-rc.1.jar` | 成功 |
| 管理端 Vite 生产静态文件 | 成功 |
| 移动端 H5/PWA 静态文件 | 成功 |
| `打金店收银台 Setup 1.0.0-rc.1.exe` | 成功 |
| Android APK | 本版本未生成，等待原生构建链与真机验收 |

## 发布检查

- 三个客户端登录页均已删除演示账号提示并清空默认账号、密码。
- 生产数据库脚本不包含用户、演示商品、库存数据或非零示例金价。
- 发布包不包含 `.env`、数据库数据目录、MinIO 数据、备份、日志、`node_modules` 或旧安装包。
- 发布包生成 SHA-256 校验清单，并执行压缩包完整性校验。

## 部署后人工验收

在目标服务器和门店设备继续执行：Docker 全服务健康、四角色权限、扫码与拍照、图片访问、商品改价实时同步、移动开单占库、收款扣库、会员刷新、回收、加工打印、断网恢复、备份与恢复演练。
